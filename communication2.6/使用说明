此版本为微信特化
config.py为配置文件,使用前需要先配置(默认的是我自己的路径)
apps处需要输入其他启动应用的路径
WeChat处需要输入微信的应用路径
programs0需要输入apps处输入的所有文件名
programs1为你想要默认关闭窗口的应用
time0为所有应用启动后关闭窗口等待的时间(默认为17秒)
time1为启动微信到按下登录按钮所需要的时间(默认为两秒)

需要第三方库:
pip install psutil -i https://pypi.org/simple
pip install pywin32
pip install pywinauto

完成后运行communication2.5.py即可启动